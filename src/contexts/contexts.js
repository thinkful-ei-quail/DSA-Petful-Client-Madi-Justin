import React from 'react';

const AppContext = React.createContext({
    pets: [],
    // removeStudent: () => {},
    // addStudent: () => {},
    // randomizeHouse: () => {},
    // getStudents: () => {}
})

export default AppContext;